import React from "react";
import s from './CallRate.module.css';

const CallRate = ({call}) => {
    return (
        <div className={s.title__item}>
            <p>Плохо</p>
        </div>
    );
};

export default CallRate;